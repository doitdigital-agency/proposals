# Do It Digital proposals

Interactive client proposal decks, one folder per client. Each deck is a self-contained index.html
with a noindex meta tag. Tag every shared link with ?v=<name> so the built-in tracker shows who viewed.

Live: https://doitdigital-agency.github.io/proposals/<client>/
